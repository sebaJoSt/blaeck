name: Host Tests

on:
  push:
    paths: ["src/**", "extras/tests/host/**", "extras/scripts/runhosttests.py", ".github/workflows/host-tests.yml", "!**/*.md"]
  pull_request:
    paths: ["src/**", "extras/tests/host/**", "extras/scripts/runhosttests.py", ".github/workflows/host-tests.yml", "!**/*.md"]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  host:
    name: Native protocol and transport suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - run: python extras/scripts/runhosttests.py
